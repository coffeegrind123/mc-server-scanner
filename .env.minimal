# Minimal setup environment variables
NODE_ENV=production

# No MONGO_URI needed - using internal Docker connection
# No RATE needed - no scanner in minimal setup
# No IDENT needed - no scanner in minimal setup

# Optional: Set timezone
TZ=UTC