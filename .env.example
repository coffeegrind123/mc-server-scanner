# MongoDB connection (for external MongoDB, otherwise Docker Compose handles internal connection)
# MONGO_URI='mongodb+srv://<username>:<password>@<mongourl>/?retryWrites=true&w=majority'

# Scanner configuration
RATE=35000
IDENT=SERVER_NAME

# Environment
NODE_ENV=production